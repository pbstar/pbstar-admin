# pbstar-admin 重构建议

> 生成日期：2026-08-07
> 用途：项目重构前的调研汇总，作为重构方案设计依据
> 范围：全仓库 82 个源文件（main 19 / components 16 / apps 30 / assets 5 / tools 6）

---

## 一、项目现状

| 项       | 现状                                                                                                                                                           |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 技术栈   | Vue 3.5 + Element Plus 2.13 + Pinia 3 + wujie 微前端 + Rsbuild 1.6                                                                                             |
| 包结构   | pnpm workspace：`main`（主应用）、`components`（组件库）、`assets`（共享资源/工具）、`tools`（CLI 与构建插件）、`apps/*`（子应用：system/example/private-app） |
| 应用管理 | `apps/apps.json` 配置驱动，CLI 负责创建/开发/构建/增删依赖                                                                                                     |
| 语言     | 纯 JS（无 TypeScript）                                                                                                                                         |

整体架构思路清晰（配置驱动多应用、共享组件库），主要问题集中在**重复代码**、**边界不清**与**全局副作用**三方面，风险可控，适合重构。

---

## 二、详细发现与建议

### 3.1 微前端架构与子应用（apps/）

**3.1.2 app-common 无包化，模板重复 4 份（P0）**

- `apps/system|example` 的 `main.js`、`App.vue` 与 template 完全一致（4 份重复），均 `import "../../app-common/appInit.js"`。
- **建议**：把 `appInit`/`useMicroApp` 收为正式 workspace 包，模板直接 `import` 包名；消除复制粘贴式的子应用初始化。

**3.1.3 apps.json 机制（P2）**

- 被 6 处消费（rsbuild.config、主路由、4 个 CLI 脚本），机制本身合理。
- 问题：端口 8801-8899 硬编码、`proUrl` 留空无校验、`appType` 在 create.mjs 里询问了却**没写入 apps.json**（后续无法区分内外应用）。
- **建议**：持久化 appType；端口改为自动分配或集中管理；proUrl 增加校验。

### 3.2 组件库（components/）

**3.2.1 废弃组件残留（P1）**

- `components/more/p-codeView/index.vue` 全项目零引用，其高亮逻辑与 `apps/example/src/views/components/` 演示页功能重复。
- **建议**：删除，或正式接入示例页。同时清理 `components/package.json` 中仅其使用的 `highlight.js` 依赖。

**3.2.2 权限逻辑耦合进基础组件（P1）**

- `components/base/p-button/index.vue` 内部直接 import `useSharedStore` 做 btnkey 权限判断 —— 最通用的基础组件不应感知业务权限。
- 且项目中仍有 5 个文件直接用 `<el-button>`（如 `apps/system/src/views/*`），绕过该封装，风格分裂。
- **建议**：权限判断抽为 `v-permission` 指令或 `usePermission` composable，组件只留 UI；统一全项目按钮出口。

**3.2.3 薄封装组件价值低（P2）**

- `p-collapse`：仅 el-collapse 加图标样式，`isCollapse` prop 初始化后不再响应（形同虚设）。
- `p-title`：手写 tab 切换，与 el-tabs 功能重复。
- `p-item`：类似 el-form-item 的布局，建议评估直接用 el-form 的 label-width。
- `p-search`：搜索表单外壳，可用 el-form inline + 折叠按钮替代。
- `p-dialog`：用 `<component :is>` 混编 ElDialog/ElDrawer 并手写全屏按钮，而 el-dialog 本身有 `fullscreen` 属性；`type="box"` 默认值语义不清。
- **建议**：逐个评估保留/删除/改为直接使用 Element Plus；保留的补齐响应式与语义化默认值。

**3.2.4 命名与归类不统一（P2）**

- 目录名混用：`p-twinBox`、`p-codeView`、`p-iconSelect`、`p-verificationCode` 用 camelCase，其余用 kebab-case。
- `page/403.vue`、`page/404.vue` 是路由页面而非组件，放在组件库且以数字命名。
- `more/` 目录语义不清（仅 appSelect.vue 一个文件）。
- **建议**：统一 kebab-case；403/404 移到各 app 的 views 下；more/ 归并入 layout/ 或改名。

**3.2.5 依赖声明混乱（P2）**

- `components/package.json` 声明 `p-export-excel` 全项目未引用；`highlight.js` 仅被废弃组件使用。
- components/assets 作为 workspace 包几乎无 dependencies，代码里 import axios/element-plus/vue 全靠根包 hoist 解析 —— pnpm 严格模式下脆弱。
- **建议**：删除无用依赖；各包声明真实依赖（或在 pnpm 中显式配置依赖提升）。

### 3.3 主应用（main/）

**3.3.1 console.warn 全局猴子补丁（P1）**

- `main/src/components/layout/layout.js:15-21`：`useNavMenu()` 每次调用都重新包一层 `console.warn`。`nav.vue`（桌面端）与 `topMobile.vue`（移动端）同时挂载会叠加两层，路由切换重新挂载继续叠加，调用栈越来越深且无法撤销。
- **建议**：改为 `main.js` 中仅执行一次的幂等拦截（Proxy 实现），或向 wujie 反馈警告根因并移除补丁。

**3.3.2 路由守卫在组件内注册（P1）**

- `main/src/views/admin/index.vue:129-141`：`router.beforeEach` 注册在 `setup()` 内且从未卸载，登录页与 admin 页往返多次后会累积多份守卫重复执行，权限校验被多次触发（内存泄漏兼逻辑隐患）。
- **建议**：抽到 `router/permission.js`，路由实例创建时只注册一次。

**3.3.3 登录/权限校验逻辑分散不一致（P1）**

- `admin/index.vue` 的 `onBeforeMount`（66-85 行）与 `router.beforeEach`（129-141 行）各写一套 token 校验、白名单判断，条件不完全一致，改一处容易漏一处。
- **建议**：收敛到单一 `auth.js` / 统一路由守卫。

**3.3.4 顶部导航逻辑重复（P2）**

- `top.vue:24-61` 与 `topMobile.vue:34-60` 的 themeChange、toLoginOut、userName/userImg watch 几乎逐行相同（约 40 行）。
- **建议**：抽 `useUserHeader()` composable（仿照已有 `useNavMenu`），仅模板差异化。

**3.3.7 其他（P3）**

- `MenuTree.vue` 硬编码三层嵌套（变量名 item/items/itemss 递增字母区分层级），无法支持第四级菜单，建议改自引用递归组件。
- `main/src/components/layout/` 下 `more/` 目录仅一个 appSelect.vue，语义不清。

### 3.4 工具链与工程配置（tools/、根目录）

**3.4.1 CLI 脚本冗余（P1）**

- `add.mjs` 与 `remove.mjs` 除命令名和 packageType 外完全相同；`dev.mjs` 与 `build.mjs` 同构。
- `create.mjs` 用了 commander 却未定义任何子命令（纯 inquirer 问答 + execSync 包一层），约 250 行仅一个动作。
- **建议**：add/remove 合并、dev/build 合并为带参数脚本；create.mjs 按子命令拆分或精简。

**3.4.2 模板拆分无必要（P2）**

- `templateMore/` 目录下仅 1 个 `.gitignore`，只在 out 类型时复制进子应用，该忽略规则对内部应用同样适用。
- **建议**：直接并入 template，删掉 templateMore。

**3.4.3 配置文件重复（P2）**

- `apps/system/jsconfig.json`、`apps/example`、`tools/template`、根目录共 4 份相同配置。
- 根 `jsconfig.json` 缺 `"@/*"` 映射（仅 @Pcomponents/@Passets），而 `main/src` 大量用 `@/` 导入，靠 `main/jsconfig.json` 兜底，IDE 提示不一致。
- **建议**：合并为根目录一份，补全 `@/*` 别名映射。

**3.4.4 rsbuild 配置风险（P2）**

- 配置内直接读 `import.meta.env.PUBLIC_TITLE`/`PUBLIC_API_BASE_URL`：配置文件跑在 Node 侧，rsbuild 不保证注入自定义 env，建议改用 `loadEnv({ mode })` 显式读取。
- 每环境 `plugins` 数组重复实例化 `checkUniqueKeyPlugin`；`distZipPlugin` 全局注册会在每个 environment 构建后各打一次 zip。
- **建议**：env 显式加载；插件实例化收敛、zip 只打一次。

### 3.5 静态资源与共享工具（assets/）

**3.5.1 死文件（P1）**

- `assets/imgs/icons14/`（add、import、close2、export、close）与 `assets/imgs/icons12/setting.png` 共 6 个文件全项目零引用。
- `assets/imgs/` 其余 3 个（sa_quan/jian_down/jian_up）仅被 `p-collapse` 使用，可改 CSS 实现后一并删除。
- `assets/utils/array.js` 的 `flatten` 无任何调用。
- **建议**：全部删除；`structure`（被 3 处使用）保留。

**3.5.2 主题状态逻辑重复（P1）**

- `assets/stores/shared.js` 的 `isDark` 与 `assets/utils/theme.js` 职责重叠：`top.vue` 改主题要同时改 DOM、store、bus 三处，`apps/app-common/useMicroApp.js` 又监听重复分发。
- **建议**：theme.js 内直接同步 store，收敛为单一入口，消除 bus 双份分发。

**3.5.3 微应用初始化模板重复（P2）**

- `main/src/main.js` 与 `apps/app-common/appInit.js` 的 createApp/ElementPlus/zhCn/CSS 引入高度重复（已暴露 createMicroApp，可进一步合并）。

**3.5.4 request.js 全局单例（P2）**

- `assets/utils/request.js` 直接对全局 `axios` 添加拦截器，未来不同请求场景（超时/鉴权策略不同）会互相干扰；`get()` 对空 url 会抛错。
- **建议**：`axios.create()` 独立实例；get 增加 url 校验。
